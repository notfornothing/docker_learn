# Portainer - Docker 容器管理工具

## 📋 目录

- [简介](#简介)
- [快速开始](#快速开始)
- [配置说明](#配置说明)
- [常用操作](#常用操作)
- [Docker Compose 命令](#docker-compose-命令)
- [参考链接](#参考链接)

---

## 📖 简介

**Portainer** 是一个轻量级的 Docker 容器管理工具，提供直观的 Web 界面来管理 Docker 容器、镜像、网络和卷。

### 主要特性

- ✅ 直观的 Web 管理界面
- ✅ 容器管理（启动、停止、重启、删除）
- ✅ 镜像管理（拉取、删除、构建）
- ✅ 网络和卷管理
- ✅ 容器日志查看
- ✅ 资源使用监控
- ✅ 多环境支持（Docker、Docker Swarm、Kubernetes）

---

## 🚀 快速开始

### 前置要求

- Docker Engine 20.10+
- Docker Compose 2.0+

### 1. 启动服务

```bash
# 进入配置目录
cd docker/portainer

# 启动服务（后台运行）
docker compose up -d

# 查看启动日志
docker compose logs -f
```

### 2. 访问管理界面

启动成功后，访问以下地址：

- **Web 界面**: http://localhost:9000
- **HTTPS 界面**: https://localhost:9443（如果配置了 SSL）

首次访问需要创建管理员账号。

### 3. 端口说明

| 端口 | 用途 | 说明 |
|------|------|------|
| 9000 | Web UI | HTTP 管理界面 |
| 9443 | Web UI (HTTPS) | HTTPS 管理界面（可选） |

---

## ⚙️ 配置说明

### 环境变量

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `TZ` | `Asia/Shanghai` | 时区设置 |

### 数据目录

```
docker/portainer/
├── docker-compose.yml    # Compose 配置文件
└── data/                 # Portainer 数据目录（自动创建）
```

### 挂载说明

- `./data:/data` - Portainer 配置和数据
- `/var/run/docker.sock:/var/run/docker.sock` - Docker socket，用于管理 Docker

---

## 🔧 常用操作

### 首次设置

1. 访问 http://localhost:9000
2. 创建管理员账号（用户名和密码）
3. 选择环境类型：
   - **Docker** - 管理本地 Docker
   - **Docker Swarm** - 管理 Swarm 集群
   - **Kubernetes** - 管理 K8s 集群

### 管理容器

1. 在左侧菜单选择 **"Containers"**
2. 查看所有容器列表
3. 可以执行以下操作：
   - 启动/停止/重启容器
   - 查看容器日志
   - 进入容器终端
   - 查看容器详情
   - 删除容器

### 管理镜像

1. 在左侧菜单选择 **"Images"**
2. 查看所有镜像
3. 可以执行以下操作：
   - 拉取新镜像
   - 删除镜像
   - 查看镜像详情
   - 构建新镜像

---

## 🐳 Docker Compose 命令

### 基本操作

```bash
# 启动服务（后台运行）
docker compose up -d

# 停止服务
docker compose down

# 重启服务
docker compose restart portainer

# 查看服务状态
docker compose ps

# 查看日志（实时）
docker compose logs -f portainer
```

### 更新服务

```bash
# 拉取最新镜像
docker compose pull

# 停止旧容器
docker compose down

# 启动新容器
docker compose up -d
```

---

## 📚 参考链接

- [官方文档](https://docs.portainer.io/)
- [GitHub 仓库](https://github.com/portainer/portainer)
- [Docker Hub](https://hub.docker.com/r/portainer/portainer-ce)

---

## ⚠️ 注意事项

1. **安全建议**
   - 首次访问后立即创建管理员账号
   - 在生产环境中使用 HTTPS（端口 9443）
   - 限制访问 IP（使用防火墙或反向代理）
   - 定期更新镜像版本

2. **Docker Socket 权限**
   - 挂载 Docker socket 需要适当的权限
   - 在生产环境中考虑使用 TLS 连接

---

**最后更新**: 2025-12-10



